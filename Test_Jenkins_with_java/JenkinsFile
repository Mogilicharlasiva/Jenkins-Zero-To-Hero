pipeline {
    agent {
	docker{
		image 'openjdk:17'}
	}

    stages {
        stage('Checkout') {
            steps {
                // Checkout the code from GitHub
                git credentialsId: 'MogilicharlaSivaGitId', url: 'https://github.com/Mogilicharlasiva/Jenkins-Zero-To-Hero.git', branch: 'main'
            }
        }
	stage('Build') {
            steps {
                // Compile the Java application
		sh '''cd Test_Jenkins_with_java/
			javac HelloWorld.java'''
            }
        }
        stage('Run') {
            steps {
                // Run the Java application
                sh '''java HelloWorld'''
            }
        }
    }
}


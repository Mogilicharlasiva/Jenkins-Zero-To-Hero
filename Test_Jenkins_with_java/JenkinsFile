pipeline {
    agent any

    stages {
        stage('Build') {
	    agent{
		docker { image 'openjdk:17'}
		}
        }
        stage('Run') {
            steps {
                // Run the Java application
                sh '''javac HelloWorld.java
                    java HelloWorld'''
            }
        }
    }
}

